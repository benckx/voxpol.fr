googleAnalytics {
  enabled = false
}

trends {
  windowDays = 40
}

staticAssets {
  minified = false
  jsCache {
    enabled = false
    maxAgeSeconds = 3600
  }
  cssCache {
    enabled = false
    maxAgeSeconds = 3600
  }
}
